[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-invite"
version = "0.1.0"
description = "Room invitation system — codes, expiration, usage limits"
license = {text = "MIT"}
requires-python = ">=3.8"
